[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "latent-gate"
version = "0.4.0"
description = "Local-first vision-language pipeline inspired by VL-JEPA. Compress images, text, conversations, and RAG documents locally via Ollama before sending to any LLM API. Includes MCP server for Claude Code, Cursor, Cline, Continue, and Zed. ~80% token savings."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Kathan Modh"}
]
keywords = [
    "vlm",
    "vision-language",
    "ollama",
    "token-optimization",
    "vl-jepa",
    "local-llm",
    "cost-reduction",
    "selective-decoding",
    "mcp",
    "model-context-protocol",
    "claude-code",
    "cursor",
    "cline",
    "prompt-compression",
    "rag",
    "ai-tools",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "black>=23.0",
    "ruff>=0.1.0",
    "mypy>=1.5",
]
mcp = [
    "mcp>=0.9.0",
]
video = [
    "opencv-python>=4.9.0",
]
embeddings = [
    "torch>=2.0.0",
    "sentence-transformers>=2.7.0",
]
all = [
    "mcp>=0.9.0",
    "opencv-python>=4.9.0",
    "torch>=2.0.0",
    "sentence-transformers>=2.7.0",
]

[project.urls]
Homepage = "https://github.com/KathanModh259/latent-gate"
Repository = "https://github.com/KathanModh259/latent-gate"
Issues = "https://github.com/KathanModh259/latent-gate/issues"
Changelog = "https://github.com/KathanModh259/latent-gate/blob/main/CHANGELOG.md"

[project.scripts]
latent-gate = "latent_gate.cli:main"

[tool.setuptools.packages.find]
include = ["latent_gate*"]
exclude = ["tests*", "examples*", "integrations*"]

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
