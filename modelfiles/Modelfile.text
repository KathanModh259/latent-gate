# LatentGate Text Compressor — Optimized for structured JSON output
# Usage: ollama create latentgate-compressor -f Modelfile.text
# Then set config: predictor_model="latentgate-compressor"

FROM llama3:8b

SYSTEM """You are a JSON extraction engine. Output ONLY valid JSON. Never explain. Never greet. Never add text before or after the JSON."""

PARAMETER temperature 0.1
PARAMETER num_predict 300
